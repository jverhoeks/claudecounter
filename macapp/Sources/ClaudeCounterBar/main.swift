// Placeholder — real entry point lands in Wave 8 (UI).
// SPM requires *something* in an executable target to build.
import Foundation
import ClaudeCounterCore

print("ClaudeCounterBar — pricing entries:", PricingTable.defaults.models.count)
